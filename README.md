```markdown
# 🛠️ AI Project Manager

**AI Project Manager** — это автономный локальный агент для разработки и научных исследований. Система сочетает в себе возможности AI-разработчика (пишет код, тестирует, ищет ошибки) и AI-исследователя (читает PDF, находит формулы, анализирует документацию).

Вместо простого чат-бота, это полноценный менеджер проектов с постоянной памятью (PostgreSQL), который умеет:
1.  **Разрабатывать код** с автоматическим планированием архитектуры.
2.  **Использовать знания** из ваших PDF-файлов (статьи, инструкции, книги) при написании кода (RAG-подход).
3.  **Безопасно работать** в изолированном окружении (Sandbox), не выходя за пределы папки проекта.

![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-18+-blue.svg)
![Ollama](https://img.shields.io/badge/Ollama-GLM--4-brightgreen.svg)
![RAG](https://img.shields.io/badge/Support-RAG_Ready-success.svg)

## 🚀 Возможности

*   **RAG (Retrieval-Augmented Generation):** Агент умеет подключать внешние папки с документацией (PDF, DOCX, TXT). Если вы попросите реализовать формулу из статьи, агент сначала найдет её в документации, а потом напишет код.
*   **Автоматическое Планирование:** Создание детальных To-Do списков и архитектуры перед написанием кода.
*   **Полноценная Разработка:** Чтение и запись файлов, поиск по коду (включая PDF, DOCX, ZIP), запуск консольных команд (npm, python, cargo...).
*   **Sandbox (Изоляция):** Агент работает строго внутри указанной директории проекта. Он не может читать `/etc` или удалять системные файлы.
*   **Умный Поиск:** Принудительный поиск в интернете (DuckDuckGo) для фактов, версий ПО и свежих новостей.
*   **Постоянная Память:** Все планы, цели и привязки к документации сохраняются в PostgreSQL.

## 📋 Требования

*   **OS:** Linux (tested on Gentoo, but works on any *nix).
*   **Python:** 3.11+
*   **RAM:** Рекомендуется 16GB+ (для комфортной работы модели GLM-4).
*   **Services:**
    *   Ollama (сервер LLM).
    *   PostgreSQL 18+.

## 🛠 Установка и Настройка

### 1. Клонирование и Виртуальное окружение

```bash
git clone <repo_url> ai_pm
cd ai_pm
```

# Создай виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate
```

### 2. Установка Python зависимостей

```bash
pip install asyncpg aiohttp aiofiles ollama duckduckgo-search beautifulsoup4 redis
```

### 3. Установка системных утилит (Gentoo)

Для работы с проектами (поиск кода, просмотр файлов) и документации нужны быстрые утилиты:

```bash
# Быстрый поиск (поддерживает поиск внутри PDF, ZIP, DOCX)
sudo emerge sys-apps/ripgrep-all

# Визуализация дерева директорий
sudo emerge app-text/tree
```

### 4. Настройка Ollama

1.  **Установка:** (если нет)
    ```bash
    curl -fsSL https://ollama.com/install.sh | sh
    ```
2.  **Запуск сервера:**
    ```bash
    ollama serve &
    ```
3.  **Вход после регистрации на сайте:**
    ```bash
    ollama signin
    ```
4.  **Загрузка модели:**
    По умолчанию скрипт настроен на `glm-4.7-flash:q4_k_m` (быстрая квантизация).
    ```bash
    ollama pull glm-4.7-flash:q4_k_m
    ```
    *Если хотите качество выше скорости, используйте `glm-4.7-flash:q8_0`.*

### 5. Настройка PostgreSQL (Важно)

Программа использует PostgreSQL для хранения памяти о проектах и путях к документации.

1.  **Установка и Инициализация:**
    ```bash
    sudo emerge --ask dev-db/postgresql
    sudo emerge --config dev-db/postgresql:18
    ```
    *(В процессе настройки просто нажимай Enter для стандартных путей).*

2.  **Запуск службы:**
    ```bash
    sudo /etc/init.d/postgresql-18 start
    sudo rc-update add postgresql-18 default
    ```

3.  **Создание Базы и Пользователя:**
    ```bash
    sudo -u postgres psql
    ```
    
    Внутри SQL-консоли:
    ```sql
    CREATE USER ai_agent WITH PASSWORD 'password';
    CREATE DATABASE ai_projects OWNER ai_agent;
    GRANT ALL PRIVILEGES ON DATABASE ai_projects TO ai_agent;
    \q
    ```
    *(Или используйте свои настройки).*

> **Примечание:** Скрипт автоматически создаст необходимые колонки (`doc_path`) в таблице при первом запуске. Никакой ручной миграции не требуется.

## 🎮 Использование

Запуск проекта:

```bash
python assistens.py
```

### Консольные команды

Интерфейс скрипта поддерживает специальные команды (слэш-команды) для управления проектами и документацией.

| Команда | Описание | Пример |
| :--- | :--- | :--- |
| **Управление Проектами** |||
| `/info  | Вызывает панель команд.` |
| `/create <name> <path> <goal>` | Создает новый проект в БД, создает папку и запускает агента. | `/create transformer ~/code "Сделать NLP модель"` |
| `/list` | Показывает список всех проектов. | `/list` |
| `/load <name>` | Загружает существующий проект из БД. | `/load transformer` |
| `/delete <name>` | Удаляет проект из БД навсегда. | `/delete old_project` |
| `/close` | Закрывает текущий проект (сохраняет прогресс). | `/close` |
| **Работа с Документацией** |||
| `/doc <path>` | Привязывает папку с документацией (PDF, книги) к текущему проекту. | `/doc /home/papers/NLP` |
| `/doc_info` | Показывает, привязана ли документация к проекту. | `/doc_info` |
| `/doc_del` | Удаляет привязку к документации. | `/doc_del` |
| **Инфо и Файлы** |||
| `/status` | Детальная информация о текущем проекте. | `/status` |
| `/files` | Быстрый просмотр структуры папок проекта (через tree). | `/files` |
| **Система** |||
| `/exit` | Завершает работу скрипта. | `/exit` |

## 🧠 Как это работает (RAG + DevOps)

Система построена на парадигме **Agentic Workflow**. Она работает в два этапа:

1.  **Исследование (Research):**
    *   Если задача сложная (нейросети, физика, редкие API), агент вызывает инструмент `search_docs`.
    *   Он сканирует папку, указанную в команде `/doc`, используя `ripgrep-all`.
    *   Агент находит нужные абзацы, формулы или определения внутри PDF.

2.  **Разработка (Development):**
    *   На основе найденного (или если задача простая), агент создает `update_project_plan`.
    *   Записывает файлы (`write_file`), ищет ошибки в коде (`search_code`), запускает тесты (`run_shell_command`).
    *   Все это происходит в **Sandbox** (папка проекта), что гарантирует безопасность.


## ⚙️ Конфигурация

Параметры задаются в начале файла `assistens.py`:

```python
# База данных
DB_USER = "ai_agent"
DB_PASS = "password"

# Модель
OLLAMA_MODEL = 'glm-4.7-flash:q4_k_m'

# Лимиты
MAX_ITERATIONS = 12    # Сколько шагов (План->Код->Дебаг) за раз
```

## ⚠️ Предупреждения

1.  **Безопасность Shell:** Агент может выполнять команды от имени пользователя, запустившего скрипт. Не запускайте от `root`. Песочница блокирует выход из папки проекта, но не защищает от `rm -rf *`.
2.  **Галлюцинации:** Несмотря на поиск в документации и интернете, LLM могут совершать ошибки. Всегда просматривайте сгенерированный код.
3.  **Документация:** Команда `/doc` не копирует файлы, а просто запоминает путь. Если вы переместите PDF в другую папку, нужно будет заново привязать путь (`/doc <новый_путь>`).

## 📝 Лицензия

MIT License. Use at your own risk.
```
